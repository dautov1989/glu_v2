# Интеграция категорий из БД в мега-меню

## ✅ Что было сделано:

### 1. **View Composer для Header**
   - Файл: `app/View/Composers/HeaderComposer.php`
   - Загружает категории из БД
   - Преобразует их в формат для Alpine.js мега-меню
   - Включает URL для каждой категории

### 2. **ViewServiceProvider**
   - Файл: `app/Providers/ViewServiceProvider.php`
   - Регистрирует HeaderComposer для компонента header
   - Зарегистрирован в `bootstrap/providers.php`

### 3. **Маршрут для категорий**
   - Файл: `routes/web.php`
   - Добавлен маршрут `/category/{slug}`
   - Использует имя `category.show`

### 4. **Обновлен Header**
   - Файл: `resources/views/components/header.blade.php`
   - Заменены статические данные на `@js($megaMenuCategories ?? [])`
   - Обновлены ссылки для использования динамических URL
   - Работает как для десктопного, так и для мобильного меню

### 5. **Страница категории**
   - Файл: `resources/views/category/show.blade.php`
   - Отображает хлебные крошки
   - Показывает подкатегории в виде карточек
   - Плейсхолдер для будущего контента

## 🎯 Как это работает:

1. При загрузке header компонента срабатывает `HeaderComposer`
2. Composer загружает корневые категории с их дочерними элементами (до 4 уровней)
3. Данные преобразуются в формат для Alpine.js
4. Мега-меню автоматически отображает категории из БД
5. При клике на категорию пользователь переходит на `/category/{slug}`
6. Страница категории показывает подкатегории и хлебные крошки

## 📊 Структура данных для меню:

```php
[
    [
        'id' => 'simptomy',
        'label' => 'Симптомы',
        'slug' => 'simptomy',
        'url' => '/category/simptomy',
        'children' => [
            [
                'label' => 'Ранние признаки',
                'slug' => 'rannie-priznaki',
                'url' => '/category/rannie-priznaki',
                'children' => [...]
            ]
        ]
    ]
]
```

## 🔄 Обновление категорий:

Категории обновляются автоматически при каждой загрузке страницы. Для кэширования можно:

1. Добавить кэширование в `HeaderComposer`:
```php
$categories = Cache::remember('mega_menu_categories', 3600, function () {
    return $this->getCategoriesTree();
});
```

2. Очищать кэш при изменении категорий:
```php
// В модели Category
protected static function boot()
{
    parent::boot();
    
    static::saved(function () {
        Cache::forget('mega_menu_categories');
    });
    
    static::deleted(function () {
        Cache::forget('mega_menu_categories');
    });
}
```

## 🌐 Проверка работы:

1. Открой главную страницу: `http://glu_v2.test`
2. Мега-меню теперь показывает категории из БД
3. Клик по любой категории ведет на `/category/{slug}`
4. На странице категории видны подкатегории и хлебные крошки

## 📝 Следующие шаги:

1. **Добавить посты/статьи** и связать их с категориями
2. **Создать админ-панель** для управления категориями
3. **Добавить кэширование** для оптимизации производительности
4. **Создать sitemap** на основе категорий для SEO
5. **Добавить фильтры** и поиск по категориям

## 🎨 Дизайн:

- Мега-меню сохранило свой оригинальный дизайн
- Добавлены динамические ссылки вместо статических `#`
- Страница категории использует тот же стиль, что и остальной сайт
- Адаптивный дизайн для мобильных устройств

## ⚡ Производительность:

- Используется eager loading (`with('childrenRecursive')`)
- Одним запросом загружается всё дерево категорий
- Рекомендуется добавить кэширование для продакшена
